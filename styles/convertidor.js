import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    formContainer: {
      margin: 10,
      padding: 10,
      borderWidth: 1,
      borderColor: 'gray',
      borderRadius: 5,
    },
    input: {
      height: 40,
      margin: 5,
      padding: 5,
      borderColor: 'gray',
      borderWidth: 1,
    },
  });
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      padding: 10,
      marginBottom: 20,
    },
    texto: {
      color: '#000000',
      fontSize: 18,
      fontWeight: 'bold',
    },
  });